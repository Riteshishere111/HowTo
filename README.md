# Git Guide


1. git fetch origin = This downloads information about the new branches from GitHub.
2. git branch -a = check what branches you have
3. git checkout -b A1 origin/A1 =  means create a new branch A1 that starts from the remote branch
